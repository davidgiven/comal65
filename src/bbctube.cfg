MEMORY {
    ZP: start = $0000, size = $0080;
    RAM: start = $0400, size = $f400;
}

SEGMENTS {
    CODE:   load = RAM, type = rw;
    RODATA: load = RAM, type = rw;
    DATA:   load = RAM, type = rw;
    BSS:    load = RAM, type = bss;
}

